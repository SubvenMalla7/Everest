@model IEnumerable<Everest_Video_Library.Models.VideoLibrary.Loan>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Create New", "Create",new { @class="btn btn-dark"})

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dvds.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Members.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dvds.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Members.FirstName)
        </td>
        <td>
          
            @Html.ActionLink("Details", "Details", new { id=item.Dvds.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Dvds.Id })
        </td>
    </tr>
}

</table>
